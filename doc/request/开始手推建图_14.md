# 开始手推建图
## 接口信息

**API Path**
/start_map

**请求协议**
WS

**自定义字段**  
**建立连接时的请求参数（websocket open）**：
**建立连接时的响应内容（websocket open）**：
**请求报文**：

**请求参数** Json
Object

| 参数名 | 说明 | 必填 | 类型 | 数据字典 |  限制 | 示例 |
| :------------ | :------------ | :------------ | :------------ | :------------ | :------------ | :------------ |
|id| |是|[number]| | |61|
|method|start_map|是|[string]| | |start_map|
|params| |是|[string]| | | |
**响应报文**：

**返回结果**
>成功 (200)
Json
Object

| 参数名  | 说明 | 必填 | 类型 | 数据字典 | 限制 | 示例 |
| :------------ | :------------ | :------------ | :------------ | :------------ | :------------ | :------------ |
|error_code| |是|[number]| ||0|
|error_message| |是|[string]| || |
|id| |是|[number]| ||23|
|params| |是|[string]| || |
|result| |是|[boolean]| ||true|

**详细说明**：
```
建图过程中想使用上一份地图，请求流程：end_map（save传false）->work_to_enter
正常保存地图：end_map（save传true）->work_to_enter
```
